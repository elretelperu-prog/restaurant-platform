import React,{useState} from 'react'; import MenuBook from './components/MenuBook.jsx'; import BottomNavigation from './components/BottomNavigation.jsx';
export default function App(){const [selected,setSelected]=useState(null),[qty,setQty]=useState(1),[count,setCount]=useState(0); const choose=d=>{setSelected(d);setQty(1)};
return <><main className="app"><div className="top"><div className="pill">Mesa 1</div><div className="brand"><b>LA TERRAZA</b><small>DEMO · PAGE CURL ENGINE</small></div><div className="pill">ES | EN</div></div><MenuBook onDish={choose}/><div className="hint">Toma una esquina de la hoja y arrástrala lentamente. El pliegue sigue tu dedo en ambos sentidos.</div></main><BottomNavigation count={count}/><div className={'modal '+(selected?'on':'')} onClick={e=>{if(e.target===e.currentTarget)setSelected(null)}}><div className="panel"><h2>{selected?.name}</h2><div className="qty"><button onClick={()=>setQty(Math.max(1,qty-1))}>−</button><b>{qty}</b><button onClick={()=>setQty(qty+1)}>+</button></div><input placeholder="Tu nombre"/><button className="confirm" onClick={()=>{setCount(count+qty);setSelected(null)}}>Añadir al pedido</button></div></div></>}
